Application
===========

The application is the main class. It's also the actual application for textual because of this all
the widgets communicate through this main application. This is done because all have a refrence to the
main application through self.app

application is located in `main.py` 