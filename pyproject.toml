[project]
name = "fire-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TalethDoesStuff", email = "sxallum@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "rstformat>=0.1.0",
    "textual>=8.2.8",
]

[project.scripts]
fire-search = "fire_search:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
